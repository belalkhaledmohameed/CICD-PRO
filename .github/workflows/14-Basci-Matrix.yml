name: 14 Basic Matrix
run-name: ${{ github.actor }} Explaining Varaibles with Practical Example Run_Number ${{ github.run_number }}

on: workflow_dispatch

jobs:
  Build:
    runs-on: ${{ matrix.os }}
    strategy:
        max-parallel: 2
        matrix:
          os: [ubuntu-latest, windows-latest]
          php_version: [8.1, 8.2, 8.3]
    steps:
      - name: Build Matrix
        run: echo "Build Project on Operating System (${{matrix.os}}) with version (${{ matrix.php_version }})"
